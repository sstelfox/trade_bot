#!/usr/bin/env ruby

base_path = File.expand_path(File.join(File.dirname(__FILE__), '../lib'))
$LOAD_PATH.unshift(base_path) unless $LOAD_PATH.include?(base_path)

require 'trade_bot'

Celluloid::Actor[:raw_stream] = TradeBot::WebSocketActor
